import { Routes } from '@angular/router';

export const routes: Routes = [
    // Currently using single-page application with all components on one page
    // Routes can be added here if you want to implement separate pages later
];
